# === PostAi / TikTok Growth Agent runtime ===
# Pin OpenAI to the new 1.x SDK (needed for `from openai import OpenAI`)
openai>=1.0.0,<2.0.0

# App framework & viz
streamlit>=1.29
altair>=5.0

# Data stack
pandas>=2.0
numpy>=1.24
python-dateutil>=2.8.2

# File formats
openpyxl>=3.1  # for reading .xlsx uploads

# Networking (fallback to direct HTTP if needed)
requests>=2.31
